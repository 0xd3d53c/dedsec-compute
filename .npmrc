# Force resolution of problematic native dependencies
legacy-peer-deps=true
strict-ssl=false
registry=https://registry.npmjs.org/

# Use npm instead of pnpm for better compatibility
package-manager=npm
